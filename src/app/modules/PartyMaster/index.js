import { connect } from 'react-redux';
import PartyMaster from './partMaster.jsx';

const mapStateToProps = (state, ownProps) => {
    return {
        
    }
  }
  
  const mapDispatchToProps = (dispatch, ownProps) => {
    return {
        
    }
  }

export const PartyMasterCont = connect(mapStateToProps,mapDispatchToProps)(PartyMaster);
